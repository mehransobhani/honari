import React from 'react';

const CartTopMenu = () => {
    return(
        <div className={['container', 'shadow-sm', 'rounded-sm'].join(' ')} style={{width: '400px', height: '188px', backgroundColor: 'white', position: 'absolute', left: '0', top: '30px', zIndex: '600'}}>

        </div>
    );
}

export default CartTopMenu;